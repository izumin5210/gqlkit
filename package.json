{
  "name": "gqlkit",
  "private": true,
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "build": "pnpm -r build",
    "check": "biome check --write",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "graphql": "^16.12.0",
    "vitest": "^3.2.0"
  }
}
