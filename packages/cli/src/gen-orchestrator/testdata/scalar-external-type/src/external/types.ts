/**
 * External date type - simulates a type from an external package.
 * This uses the same base type (Date) that our scalar maps to.
 */
export type ExternalDate = Date;

/**
 * External timestamp type - type alias for Date.
 */
export type ExternalTimestamp = Date;
