/**
 * External User type from a different package (e.g., Prisma, protobuf-es)
 * This has the same name as the schema User but is a different type.
 */
export interface User {
  id: string;
  email: string;
  createdAt: string;
}
