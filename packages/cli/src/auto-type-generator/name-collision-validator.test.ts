import { describe, expect, it } from "vitest";
import type {
  AutoGeneratedType,
  GeneratedFromInfo,
} from "./auto-type-generator.js";
import {
  type NameCollisionValidatorInput,
  validateNameCollisions,
} from "./name-collision-validator.js";

function createMockAutoGeneratedType(
  name: string,
  generatedFrom: GeneratedFromInfo,
  sourceFile = "src/gqlkit/schema/types.ts",
  line = 1,
): AutoGeneratedType {
  return {
    name,
    kind: "Object",
    fields: [],
    enumValues: null,
    unionMembers: null,
    needsStringEnumMapping: false,
    sourceLocation: { file: sourceFile, line, column: 1 },
    generatedFrom,
    description: null,
  };
}

function createMockAutoGeneratedEnumType(
  name: string,
  generatedFrom: GeneratedFromInfo,
  sourceFile = "src/gqlkit/schema/types.ts",
  line = 1,
): AutoGeneratedType {
  return {
    name,
    kind: "Enum",
    fields: null,
    enumValues: [
      {
        name: "FOO",
        originalValue: "foo",
        description: null,
        deprecated: null,
      },
      {
        name: "BAR",
        originalValue: "bar",
        description: null,
        deprecated: null,
      },
    ],
    unionMembers: null,
    needsStringEnumMapping: true,
    sourceLocation: { file: sourceFile, line, column: 1 },
    generatedFrom,
    description: null,
  };
}

function createMockAutoGeneratedUnionType(
  name: string,
  generatedFrom: GeneratedFromInfo,
  unionMembers: string[],
  sourceFile = "src/gqlkit/schema/types.ts",
  line = 1,
): AutoGeneratedType {
  return {
    name,
    kind: "Union",
    fields: null,
    enumValues: null,
    unionMembers,
    needsStringEnumMapping: false,
    sourceLocation: { file: sourceFile, line, column: 1 },
    generatedFrom,
    description: null,
  };
}

function createMockAutoGeneratedOneOfInputType(
  name: string,
  generatedFrom: GeneratedFromInfo,
  sourceFile = "src/gqlkit/schema/types.ts",
  line = 1,
): AutoGeneratedType {
  return {
    name,
    kind: "OneOfInputObject",
    fields: [
      {
        name: "optionA",
        type: {
          typeName: "String",
          nullable: true,
          list: false,
          listItemNullable: null,
        },
        description: null,
        deprecated: null,
        directives: null,
        defaultValue: null,
      },
      {
        name: "optionB",
        type: {
          typeName: "Int",
          nullable: true,
          list: false,
          listItemNullable: null,
        },
        description: null,
        deprecated: null,
        directives: null,
        defaultValue: null,
      },
    ],
    enumValues: null,
    unionMembers: null,
    needsStringEnumMapping: false,
    sourceLocation: { file: sourceFile, line, column: 1 },
    generatedFrom,
    description: null,
  };
}

describe("validateNameCollisions", () => {
  describe("user-defined type collision detection", () => {
    it("detects collision between auto-generated type and user-defined type", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [
          {
            name: "UserProfile",
            sourceLocation: {
              file: "src/gqlkit/schema/user.ts",
              line: 10,
              column: 1,
            },
          },
        ],
        autoGeneratedTypes: [
          createMockAutoGeneratedType(
            "UserProfile",
            {
              parentTypeName: "User",
              fieldPath: ["profile"],
              context: "typeField",
            },
            "src/gqlkit/schema/types.ts",
            5,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_USER",
        severity: "error",
      });
      expect(result.diagnostics[0]?.message).toContain("UserProfile");
      expect(result.diagnostics[0]?.message).toContain("user-defined type");
    });

    it("includes source locations in diagnostic message", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [
          {
            name: "UserProfile",
            sourceLocation: {
              file: "src/gqlkit/schema/user.ts",
              line: 10,
              column: 1,
            },
          },
        ],
        autoGeneratedTypes: [
          createMockAutoGeneratedType(
            "UserProfile",
            {
              parentTypeName: "User",
              fieldPath: ["profile"],
              context: "typeField",
            },
            "src/gqlkit/schema/types.ts",
            5,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.diagnostics[0]?.message).toContain(
        "src/gqlkit/schema/user.ts",
      );
    });

    it("includes recommended resolution in diagnostic message", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [
          {
            name: "UserProfile",
            sourceLocation: {
              file: "src/gqlkit/schema/user.ts",
              line: 10,
              column: 1,
            },
          },
        ],
        autoGeneratedTypes: [
          createMockAutoGeneratedType("UserProfile", {
            parentTypeName: "User",
            fieldPath: ["profile"],
            context: "typeField",
          }),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      const message = result.diagnostics[0]?.message ?? "";
      expect(
        message.includes("renaming") || message.includes("explicit type"),
      ).toBe(true);
    });

    it("returns no collisions when types do not conflict", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [
          {
            name: "User",
            sourceLocation: {
              file: "src/gqlkit/schema/user.ts",
              line: 10,
              column: 1,
            },
          },
        ],
        autoGeneratedTypes: [
          createMockAutoGeneratedType("UserProfile", {
            parentTypeName: "User",
            fieldPath: ["profile"],
            context: "typeField",
          }),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(false);
      expect(result.diagnostics).toHaveLength(0);
    });

    it("detects multiple user-defined type collisions", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [
          {
            name: "UserProfile",
            sourceLocation: {
              file: "src/gqlkit/schema/user.ts",
              line: 10,
              column: 1,
            },
          },
          {
            name: "UserAddress",
            sourceLocation: {
              file: "src/gqlkit/schema/user.ts",
              line: 20,
              column: 1,
            },
          },
        ],
        autoGeneratedTypes: [
          createMockAutoGeneratedType("UserProfile", {
            parentTypeName: "User",
            fieldPath: ["profile"],
            context: "typeField",
          }),
          createMockAutoGeneratedType("UserAddress", {
            parentTypeName: "User",
            fieldPath: ["address"],
            context: "typeField",
          }),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(2);
      expect(
        result.diagnostics.every(
          (d) => d.code === "AUTO_TYPE_NAME_COLLISION_USER",
        ),
      ).toBe(true);
    });
  });

  describe("auto-generated type collision detection", () => {
    it("detects collision between multiple auto-generated types with same name", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedType(
            "UpdateDataInput",
            {
              parentTypeName: null,
              fieldPath: ["data"],
              context: "resolverArg",
            },
            "src/gqlkit/schema/mutation.ts",
            5,
          ),
          createMockAutoGeneratedType(
            "UpdateDataInput",
            {
              parentTypeName: null,
              fieldPath: ["data"],
              context: "resolverArg",
            },
            "src/gqlkit/schema/query.ts",
            10,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_AUTO",
        severity: "error",
      });
    });

    it("lists all collision source locations in diagnostic message", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedType(
            "UpdateDataInput",
            {
              parentTypeName: null,
              fieldPath: ["data"],
              context: "resolverArg",
            },
            "src/gqlkit/schema/mutation.ts",
            5,
          ),
          createMockAutoGeneratedType(
            "UpdateDataInput",
            {
              parentTypeName: null,
              fieldPath: ["data"],
              context: "resolverArg",
            },
            "src/gqlkit/schema/query.ts",
            10,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      const message = result.diagnostics[0]?.message ?? "";
      expect(message).toContain("src/gqlkit/schema/mutation.ts");
      expect(message).toContain("src/gqlkit/schema/query.ts");
    });

    it("includes recommended resolution in auto-collision diagnostic", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedType(
            "UpdateDataInput",
            {
              parentTypeName: null,
              fieldPath: ["data"],
              context: "resolverArg",
            },
            "src/gqlkit/schema/mutation.ts",
            5,
          ),
          createMockAutoGeneratedType(
            "UpdateDataInput",
            {
              parentTypeName: null,
              fieldPath: ["data"],
              context: "resolverArg",
            },
            "src/gqlkit/schema/query.ts",
            10,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      const message = result.diagnostics[0]?.message ?? "";
      expect(message.includes("explicit type")).toBe(true);
    });

    it("returns no collisions when auto-generated types have unique names", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedType("UserProfile", {
            parentTypeName: "User",
            fieldPath: ["profile"],
            context: "typeField",
          }),
          createMockAutoGeneratedType("UserAddress", {
            parentTypeName: "User",
            fieldPath: ["address"],
            context: "typeField",
          }),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(false);
      expect(result.diagnostics).toHaveLength(0);
    });

    it("handles three or more auto-generated types with same name", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedType(
            "DataInput",
            {
              parentTypeName: null,
              fieldPath: [],
              context: "resolverArg",
            },
            "src/gqlkit/schema/a.ts",
            1,
          ),
          createMockAutoGeneratedType(
            "DataInput",
            {
              parentTypeName: null,
              fieldPath: [],
              context: "resolverArg",
            },
            "src/gqlkit/schema/b.ts",
            2,
          ),
          createMockAutoGeneratedType(
            "DataInput",
            {
              parentTypeName: null,
              fieldPath: [],
              context: "resolverArg",
            },
            "src/gqlkit/schema/c.ts",
            3,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      const message = result.diagnostics[0]?.message ?? "";
      expect(message).toContain("src/gqlkit/schema/a.ts");
      expect(message).toContain("src/gqlkit/schema/b.ts");
      expect(message).toContain("src/gqlkit/schema/c.ts");
    });
  });

  describe("combined collision scenarios", () => {
    it("detects both user-defined and auto-generated collisions", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [
          {
            name: "UserProfile",
            sourceLocation: {
              file: "src/gqlkit/schema/user.ts",
              line: 10,
              column: 1,
            },
          },
        ],
        autoGeneratedTypes: [
          createMockAutoGeneratedType("UserProfile", {
            parentTypeName: "User",
            fieldPath: ["profile"],
            context: "typeField",
          }),
          createMockAutoGeneratedType(
            "DataInput",
            {
              parentTypeName: null,
              fieldPath: [],
              context: "resolverArg",
            },
            "src/gqlkit/schema/a.ts",
            1,
          ),
          createMockAutoGeneratedType(
            "DataInput",
            {
              parentTypeName: null,
              fieldPath: [],
              context: "resolverArg",
            },
            "src/gqlkit/schema/b.ts",
            2,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(2);
      expect(
        result.diagnostics.some(
          (d) => d.code === "AUTO_TYPE_NAME_COLLISION_USER",
        ),
      ).toBe(true);
      expect(
        result.diagnostics.some(
          (d) => d.code === "AUTO_TYPE_NAME_COLLISION_AUTO",
        ),
      ).toBe(true);
    });
  });

  describe("enum type collision detection", () => {
    it("detects collision between auto-generated enum type and user-defined type", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [
          {
            name: "UserStatus",
            sourceLocation: {
              file: "src/gqlkit/schema/user.ts",
              line: 10,
              column: 1,
            },
          },
        ],
        autoGeneratedTypes: [
          createMockAutoGeneratedEnumType(
            "UserStatus",
            {
              parentTypeName: "User",
              fieldPath: ["status"],
              context: "typeField",
            },
            "src/gqlkit/schema/types.ts",
            5,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_USER",
        severity: "error",
      });
      expect(result.diagnostics[0]?.message).toContain("UserStatus");
    });

    it("detects collision between auto-generated enum and auto-generated object type", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedEnumType(
            "UserProfile",
            {
              parentTypeName: "User",
              fieldPath: ["profile"],
              context: "typeField",
            },
            "src/gqlkit/schema/a.ts",
            5,
          ),
          createMockAutoGeneratedType(
            "UserProfile",
            {
              parentTypeName: "User",
              fieldPath: ["profile"],
              context: "typeField",
            },
            "src/gqlkit/schema/b.ts",
            10,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_AUTO",
        severity: "error",
      });
    });

    it("detects collision between multiple auto-generated enum types", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedEnumType(
            "StatusInput",
            {
              parentTypeName: null,
              fieldPath: ["status"],
              context: "resolverArg",
            },
            "src/gqlkit/schema/mutation.ts",
            5,
          ),
          createMockAutoGeneratedEnumType(
            "StatusInput",
            {
              parentTypeName: null,
              fieldPath: ["status"],
              context: "resolverArg",
            },
            "src/gqlkit/schema/query.ts",
            10,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_AUTO",
        severity: "error",
      });
      const message = result.diagnostics[0]?.message ?? "";
      expect(message).toContain("src/gqlkit/schema/mutation.ts");
      expect(message).toContain("src/gqlkit/schema/query.ts");
    });

    it("returns no collisions when enum types have unique names", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedEnumType("UserStatus", {
            parentTypeName: "User",
            fieldPath: ["status"],
            context: "typeField",
          }),
          createMockAutoGeneratedEnumType("PostCategory", {
            parentTypeName: "Post",
            fieldPath: ["category"],
            context: "typeField",
          }),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(false);
      expect(result.diagnostics).toHaveLength(0);
    });
  });

  describe("edge cases", () => {
    it("handles empty inputs", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(false);
      expect(result.diagnostics).toHaveLength(0);
    });

    it("is case-sensitive for type names", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [
          {
            name: "UserProfile",
            sourceLocation: {
              file: "src/gqlkit/schema/user.ts",
              line: 10,
              column: 1,
            },
          },
        ],
        autoGeneratedTypes: [
          createMockAutoGeneratedType("Userprofile", {
            parentTypeName: "User",
            fieldPath: ["profile"],
            context: "typeField",
          }),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(false);
      expect(result.diagnostics).toHaveLength(0);
    });
  });

  describe("union type collision detection", () => {
    it("detects collision between auto-generated union type and user-defined type", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [
          {
            name: "SearchResultItem",
            sourceLocation: {
              file: "src/gqlkit/schema/search.ts",
              line: 10,
              column: 1,
            },
          },
        ],
        autoGeneratedTypes: [
          createMockAutoGeneratedUnionType(
            "SearchResultItem",
            {
              parentTypeName: "SearchResult",
              fieldPath: ["item"],
              context: "typeField",
            },
            ["User", "Post"],
            "src/gqlkit/schema/types.ts",
            5,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_USER",
        severity: "error",
      });
      expect(result.diagnostics[0]?.message).toContain("SearchResultItem");
    });

    it("detects collision between multiple auto-generated union types with same name", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedUnionType(
            "ResultItem",
            {
              parentTypeName: "SearchResult",
              fieldPath: ["item"],
              context: "typeField",
            },
            ["User", "Post"],
            "src/gqlkit/schema/search.ts",
            5,
          ),
          createMockAutoGeneratedUnionType(
            "ResultItem",
            {
              parentTypeName: "QueryResult",
              fieldPath: ["item"],
              context: "typeField",
            },
            ["Product", "Category"],
            "src/gqlkit/schema/query.ts",
            10,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_AUTO",
        severity: "error",
      });
      const message = result.diagnostics[0]?.message ?? "";
      expect(message).toContain("src/gqlkit/schema/search.ts");
      expect(message).toContain("src/gqlkit/schema/query.ts");
    });

    it("returns no collisions when union types have unique names", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedUnionType(
            "SearchResultItem",
            {
              parentTypeName: "SearchResult",
              fieldPath: ["item"],
              context: "typeField",
            },
            ["User", "Post"],
          ),
          createMockAutoGeneratedUnionType(
            "QueryResultItem",
            {
              parentTypeName: "QueryResult",
              fieldPath: ["item"],
              context: "typeField",
            },
            ["Product", "Category"],
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(false);
      expect(result.diagnostics).toHaveLength(0);
    });
  });

  describe("oneOf input object type collision detection", () => {
    it("detects collision between auto-generated oneOf type and user-defined type", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [
          {
            name: "CreateMessageRecipientInput",
            sourceLocation: {
              file: "src/gqlkit/schema/message.ts",
              line: 10,
              column: 1,
            },
          },
        ],
        autoGeneratedTypes: [
          createMockAutoGeneratedOneOfInputType(
            "CreateMessageRecipientInput",
            {
              parentTypeName: "CreateMessage",
              fieldPath: ["recipient"],
              context: "inputField",
            },
            "src/gqlkit/schema/types.ts",
            5,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_USER",
        severity: "error",
      });
      expect(result.diagnostics[0]?.message).toContain(
        "CreateMessageRecipientInput",
      );
    });

    it("detects collision between multiple auto-generated oneOf types with same name", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedOneOfInputType(
            "TargetInput",
            {
              parentTypeName: "CreateMessage",
              fieldPath: ["target"],
              context: "inputField",
            },
            "src/gqlkit/schema/message.ts",
            5,
          ),
          createMockAutoGeneratedOneOfInputType(
            "TargetInput",
            {
              parentTypeName: "SendNotification",
              fieldPath: ["target"],
              context: "inputField",
            },
            "src/gqlkit/schema/notification.ts",
            10,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_AUTO",
        severity: "error",
      });
      const message = result.diagnostics[0]?.message ?? "";
      expect(message).toContain("src/gqlkit/schema/message.ts");
      expect(message).toContain("src/gqlkit/schema/notification.ts");
    });

    it("returns no collisions when oneOf types have unique names", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedOneOfInputType("CreateMessageRecipientInput", {
            parentTypeName: "CreateMessage",
            fieldPath: ["recipient"],
            context: "inputField",
          }),
          createMockAutoGeneratedOneOfInputType("SendNotificationTargetInput", {
            parentTypeName: "SendNotification",
            fieldPath: ["target"],
            context: "inputField",
          }),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(false);
      expect(result.diagnostics).toHaveLength(0);
    });
  });

  describe("cross-type collision detection", () => {
    it("detects collision between union type and oneOf input type", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedUnionType(
            "ItemResult",
            {
              parentTypeName: "Search",
              fieldPath: ["item"],
              context: "typeField",
            },
            ["User", "Post"],
            "src/gqlkit/schema/search.ts",
            5,
          ),
          createMockAutoGeneratedOneOfInputType(
            "ItemResult",
            {
              parentTypeName: "Query",
              fieldPath: ["item"],
              context: "inputField",
            },
            "src/gqlkit/schema/query.ts",
            10,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_AUTO",
        severity: "error",
      });
    });

    it("detects collision between union type and object type", () => {
      const input: NameCollisionValidatorInput = {
        userDefinedTypes: [],
        autoGeneratedTypes: [
          createMockAutoGeneratedUnionType(
            "UserItem",
            {
              parentTypeName: "Search",
              fieldPath: ["item"],
              context: "typeField",
            },
            ["Admin", "Guest"],
            "src/gqlkit/schema/search.ts",
            5,
          ),
          createMockAutoGeneratedType(
            "UserItem",
            {
              parentTypeName: "User",
              fieldPath: ["item"],
              context: "typeField",
            },
            "src/gqlkit/schema/user.ts",
            10,
          ),
        ],
        sourceRoot: null,
      };

      const result = validateNameCollisions(input);

      expect(result.hasCollisions).toBe(true);
      expect(result.diagnostics).toHaveLength(1);
      expect(result.diagnostics[0]).toMatchObject({
        code: "AUTO_TYPE_NAME_COLLISION_AUTO",
        severity: "error",
      });
    });
  });
});
